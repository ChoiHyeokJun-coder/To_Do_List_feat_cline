# Microsoft To Do 스타일 할 일 관리 앱

Vite + React + Tailwind CSS로 만든 Microsoft To Do 스타일의 할 일 관리 애플리케이션입니다.

## 🎯 주요 기능

### 📝 할 일 관리
- ✅ 할 일 추가/수정/삭제/완료
- ✅ 할 일 제목 더블클릭으로 인라인 수정
- ✅ 동그란 체크박스로 완료/미완료 토글
- ✅ 완료된 할 일에 취소선 표시

### 📋 세부 단계 (Sub-tasks)
- ✅ 세부 단계 추가/완료/삭제
- ✅ 모든 세부 단계 완료 시 할 일 자동 완료
- ✅ 세부 단계 전체 완료/삭제 버튼
- ✅ 세부 단계 진행률 표시

### 📅 일정 관리
- ✅ 마감일 설정 기능
- ✅ "나의 하루" 기능 (오늘 마감일이거나 수동으로 추가한 항목 표시)
- ✅ "나의 하루"에 할 일 추가 토글

### 🎨 사용자 인터페이스
- ✅ Microsoft To Do 스타일 디자인 (화이트 배경 + 파란색 포인트)
- ✅ 다크 모드 지원 (밝은 회색 톤)
- ✅ 반응형 UI (모바일/태블릿/데스크탑)
- ✅ 부드러운 애니메이션 및 전환 효과

### 💾 데이터 관리
- ✅ LocalStorage 자동 저장 (새로고침해도 데이터 유지)
- ✅ 다크 모드 설정 저장

### 🔧 편의 기능
- ✅ 전체 할 일 완료/삭제 버튼
- ✅ 사이드바 메뉴 ("나의 하루", "전체 할 일")
- ✅ 상세 패널에서 모든 정보 관리

## 📱 반응형 디자인

### 모바일 (< 768px)
- 상단에 드롭다운 메뉴와 다크모드 토글
- 사이드바 숨김
- 상세 패널은 하단에서 올라오는 시트 형태 (최대 높이 80%)

### 태블릿 (768px ~ 1024px)
- 사이드바 표시
- 상세 패널은 하단 시트 형태

### 데스크탑 (> 1024px)
- 사이드바 + 할 일 목록 + 상세 패널 3단 레이아웃
- 모든 기능을 한 화면에서 확인 가능

## 🚀 시작하기

### 📌 중요: 로컬 애플리케이션입니다
이 프로젝트는 **각 사용자의 컴퓨터에서 실행되는 로컬 애플리케이션**입니다.
- `localhost:5173`은 **본인의 컴퓨터**에서만 접속 가능합니다
- 다른 사람이 사용하려면 **각자의 컴퓨터에 설치**해야 합니다
- 데이터는 각 사용자의 브라우저 LocalStorage에 저장됩니다

### 필수 요구사항
- Node.js (LTS 버전 권장) - [다운로드](https://nodejs.org/)
- npm (Node.js 설치 시 자동 포함)

### 설치 및 실행 방법

#### 방법 1: 명령어로 실행 (권장)

1. **프로젝트 다운로드**
```bash
# Git으로 클론하기
git clone <repository-url>
cd MyToDo_App

# 또는 ZIP 파일 다운로드 후 압축 해제
```

2. **의존성 설치** (최초 1회만)
```bash
npm install
```

3. **개발 서버 실행**
```bash
npm run dev
```

4. **브라우저에서 확인**
- 터미널에 표시된 주소로 접속 (보통 http://localhost:5173)
- 또는 브라우저에서 직접 `http://localhost:5173` 입력

#### 방법 2: 배치 파일로 실행 (Windows 사용자)

1. **install.bat 더블클릭** (최초 1회만)
   - 필요한 패키지를 자동으로 설치합니다

2. **dev.bat 더블클릭**
   - 개발 서버가 시작됩니다
   - 브라우저에서 http://localhost:5173 으로 접속

#### 서버 종료
- 터미널에서 `Ctrl + C` 누르기

### 다른 사람과 공유하는 방법

#### 옵션 1: 소스 코드 공유
1. 이 프로젝트 폴더를 ZIP으로 압축
2. 상대방에게 전달
3. 상대방이 위의 "설치 및 실행 방법" 따라하기

#### 옵션 2: 웹 호스팅 (인터넷에 배포)
```bash
# 1. 프로덕션 빌드 생성
npm run build

# 2. dist 폴더를 다음 서비스에 업로드
# - Vercel (https://vercel.com) - 무료, 추천
# - Netlify (https://netlify.com) - 무료
# - GitHub Pages
```

배포 후에는 인터넷 주소(예: https://your-app.vercel.app)로 누구나 접속 가능합니다.

### 프로덕션 빌드

```bash
# 빌드 생성
npm run build

# 빌드 미리보기
npm run preview
```

빌드된 파일은 `dist` 폴더에 생성됩니다.

## 🛠️ 기술 스택

- **프레임워크**: React 18
- **빌드 도구**: Vite 6
- **스타일링**: Tailwind CSS 3
- **상태 관리**: React Hooks (useState, useEffect)
- **데이터 저장**: LocalStorage API

## 📂 프로젝트 구조

```
MyToDo_App/
├── src/
│   ├── components/
│   │   ├── Sidebar.jsx          # 사이드바 (메뉴)
│   │   ├── TodoList.jsx          # 할 일 목록
│   │   ├── TodoItem.jsx          # 개별 할 일 항목
│   │   └── DetailPanel.jsx       # 상세 정보 패널
│   ├── App.jsx                   # 메인 앱 컴포넌트
│   ├── main.jsx                  # 앱 진입점
│   └── index.css                 # 전역 스타일
├── index.html
├── package.json
├── vite.config.js
├── tailwind.config.js
└── postcss.config.js
```

## 💡 개발 과정 (프롬프트 로그)

### 1. 초기 프로젝트 설정
```
너는 지금부터 유능한 프론트엔드 개발자야. 현재 열려있는 폴더에 Vite, React, Tailwind CSS를 사용해서 새로운 프로젝트를 초기화(install)하고, Microsoft To Do 스타일의 할 일 관리 앱을 만들어줘.
```

### 2. UI 및 기본 기능 구현
```
화면 왼쪽에는 사이드바가 있고 '나의 하루', '전체 할 일' 메뉴가 있어야 해.
메인 화면에서는 할 일을 입력하고 추가하는 텍스트 인풋과 버튼이 있어야 해.
할 일 목록 옆에는 동그란 체크박스가 있어서 누르면 완료선(취소선)이 그어지게 해.
전체적인 색상은 깔끔한 화이트 배경에 마이크로소프트 특유의 파란색(Blue) 포인트 컬러를 사용해줘.
```

### 3. 상세 기능 추가
```
특정 할 일을 클릭하면 오른쪽에 상세 패널이 열리고, 여기서 '세부 단계(Sub-tasks)' 추가, '마감일' 설정 기능을 구현해줘.
사이드바의 '나의 하루'를 클릭하면, 마감일이 오늘이거나 별도로 지정된 항목만 필터링해서 보여주는 로직을 작성해줘.
```

### 4. 버그 수정 및 개선
```
나의 하루에서 할일 목록이 추가되지 않는 문제가 있어
하단부분에 전체 완료, 전체 삭제 칸을 넣어주고 할일을 수정가능하게 만들어줘
```

### 5. 세부 단계 기능 개선
```
세부 단계를 체크할때 체크가 안되고 있고 세부단계에도 전체 완료와 같은 기능을 넣어줘
또한 다크모드도 설정할수 있게 만들어 줬으면 해
```

### 6. 다크모드 밝기 조정
```
세부단계에 체크박스를 누르면 완료는 되었다고 나오는데 체크가 되었다는 표시가 되지않아 이 기능을 추가해주고
다크모드가 너무 어두워서 조금만 밝게 만들어줘
```

### 7. 즉시 업데이트 기능
```
세부단계를 추가했을때 바로 표시되게 만들어줘
```

### 8. 고급 기능 추가
```
세부 단계를 추가했을때 전부 완료되면 할일이 자동적으로 완료되게 만들어줘

바이브코딩 입문자들도 쉽게 알수 있게 주석을 만들어주고
새로고침시에도 이전에 데이터가 그대로 남아있게 Local Storage에 저장할수 있게 만들어주고
반응형 UI를 만들때 모바일 UI, 데스크탑 UI, 패드 UI를 구분해서 만들어
```

### 9. 모바일 UX 개선
```
모바일용 UI로 될때는 세부정보가 하단부분에 뜨게 만들어서 할일목록이 가려지지 않게 해줘
```

## 🎨 디자인 특징

### 색상 팔레트
- **Primary Blue**: `#0078D4` (Microsoft Blue)
- **Hover Blue**: `#106EBE`
- **Light Blue**: `#E1F5FE`
- **다크 모드**: Gray-700 ~ Gray-800 (밝은 회색 톤)

### 주요 UI 요소
- 둥근 체크박스 (완료 시 파란색 배경 + 흰색 체크)
- 부드러운 호버 효과
- 카드 스타일 할 일 항목
- 모바일 하단 시트 (Bottom Sheet)

## 📝 코드 특징

### 초보자 친화적
- 모든 함수와 상태에 한글 주석 추가
- JSDoc 스타일 매개변수 설명
- 명확한 변수명과 함수명

### React 모범 사례
- 함수형 컴포넌트 사용
- React Hooks 활용 (useState, useEffect)
- Props를 통한 데이터 전달
- 이벤트 핸들러 분리

### Tailwind CSS 활용
- 유틸리티 클래스 기반 스타일링
- 반응형 브레이크포인트 (sm, md, lg)
- 다크 모드 클래스 (dark:)
- 커스텀 색상 정의

## 🔍 주요 함수 설명

### `addTodo(title)`
새로운 할 일을 추가합니다. "나의 하루" 뷰에서 추가하면 자동으로 `isMyDay: true`로 설정됩니다.

### `toggleSubtask(todoId, subtaskId)`
세부 단계를 체크/해제합니다. 모든 세부 단계가 완료되면 할 일도 자동으로 완료 처리됩니다.

### `getFilteredTodos()`
현재 보기 모드에 따라 할 일 목록을 필터링합니다.
- `'all'`: 모든 할 일 반환
- `'myday'`: 오늘 마감일이거나 `isMyDay: true`인 항목만 반환

### 🔍 검색 및 필터링
- ✅ 할 일 검색 기능 (제목 및 태그)
- ✅ 카테고리 필터 (개인, 업무, 쇼핑, 기타)
- ✅ 정렬 옵션 (이름순, 마감일순, 생성일순, 우선순위순)

### 🏷️ 카테고리 및 태그
- ✅ 할 일 카테고리 설정
- ✅ 태그 추가/삭제 기능
- ✅ 카테고리별 색상 배지

### ⚡ 우선순위 및 반복
- ✅ 3단계 우선순위 (낮음, 보통, 높음)
- ✅ 반복 할 일 설정 (매일, 매주, 매월)
- ✅ 우선순위별 색상 배지

## 🌟 향후 개선 사항

- [ ] 데이터 내보내기/가져오기
- [ ] 클라우드 동기화
- [ ] 할 일 통계 및 분석
- [ ] 알림 기능

## 📄 라이선스

이 프로젝트는 학습 목적으로 만들어졌습니다.

## 👨‍💻 개발자

AI 어시스턴트와 함께 개발된 프로젝트입니다.

---

**Made with ❤️ using Vite + React + Tailwind CSS**
